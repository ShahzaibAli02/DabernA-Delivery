<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daber.daberna">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Login.Login"></activity>
        <activity android:name=".Activities.Driver.GotoDestination" />
        <activity android:name=".Activities.Driver.GetPassenger" />
        <activity android:name=".Activities.Driver.SearchPassenger" />
        <activity android:name=".Activities.Passenger.DriverOnWay" />
        <activity android:name=".Activities.Passenger.RequestingDriver" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Activities.Passenger.Destination_Selector" />
        <activity android:name=".Activities.Login.PassengerRegistation" />
        <activity android:name=".Activities.Login.DriverRegistration" />
        <activity android:name=".Activities.MapsActivity" />
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>